#include <bits/stdc++.h>
using namespace std;

/**
typedef struct stu{
    string str;
    int pro_sum;
    //int nm[pro_sum];
}STU;
int main()
{
    int student,problem_number,flag_num;
    map<int,int> pro_num;
    scanf("%d%d%d",student,problem_number,flag_num);
    for(int i=0;i<problem_number;i++){
        int x;
        scanf("%d", &x);
        pro_num[i]=x;
    }
    int *a = (STU *)malloc(student * sizeof(STU) );

    return 0;
}

*/
